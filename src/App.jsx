import MSGroup from './components/MSGroup'; // Import the new component

function App() {
  return (
    // App acts as the main container and renders the MSGroup component
    <MSGroup />
  );
}

export default App;